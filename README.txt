Want to run it? 
- install the libaries (sorry! I will make a requirements.txt)
- put in your GPT API key (you can't use mine... takes money but extremely cheap)
- npm run dev (run frontend)
- uvicorn app:app --reload (run backend; start asgi server)